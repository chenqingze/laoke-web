import { MucHeaderPipe } from './muc-header.pipe';

describe('MucHeaderPipe', () => {
  it('create an instance', () => {
    const pipe = new MucHeaderPipe();
    expect(pipe).toBeTruthy();
  });
});
